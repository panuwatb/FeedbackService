{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AppSettings": {
    "KeyVaultName": "abc",
    "ByPassKeyVault": "false"
  },
  "NLog": {
    "throwConfigExceptions": true,
    "variables": {
      "commonLayout": "${date:format=yyyy-MM-dd HH\\:mm\\:ss.fff} ${machinename} ${level:uppercase=true} ${message} ${when:when=level == LogLevel.Error}",
      "logDir": "C:\\Logs\\FeedbackService",
      "customFileName": "${level}"
    },
    "targets": {
      "logfile": {
        "type": "File",
        "filename": "${logDir}/${customFileName}.log",
        "layout": "${commonLayout}",
        "archiveFileName": "${logDir}/${customFileName}.${shortdate}.{#####}.log",
        "archiveAboveSize": "10485760",
        "archiveNumbering": "Sequence",
        "concurrentWrites": "true"
      },
      "logconsole": {
        "type": "Console",
        "layout": "${commonLayout}"
      }
    },
    "rules": [
      {
        "logger": "*",
        "minLevel": "Trace",
        "writeTo": "logconsole"
      },
      {
        "logger": "*",
        "minLevel": "Trace",
        "writeTo": "logfile"
      }
    ]
  }
}
